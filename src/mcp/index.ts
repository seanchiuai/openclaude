/**
 * MCP server module.
 *
 * The gateway-tools-server.ts is a standalone entry point spawned as a subprocess.
 * This index re-exports nothing at runtime — it exists so the build config
 * can reference src/mcp/gateway-tools-server.ts as an entry point.
 */
